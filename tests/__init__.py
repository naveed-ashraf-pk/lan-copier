"""lan-copier test suite.

Split per module under tests/ so each subsystem's tests stay focused and
maintainable. `python3 tests.py` (here) is the single runner that executes
every module's ALL_TESTS, then the UI smoke test.
"""